apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: '{{ include "common.names.fullname" . }}'
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
data:
  htpasswd: {{ .Values.store_dav.auth_htpasswd | b64enc | quote }}
